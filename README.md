# 6123_two_stage_model

## Environment
- Python verson and all installed packages are in the `environment.yml` file. One can reproduce the exact environment by inputting `conda env create -f environment.yml` in the terminal.
